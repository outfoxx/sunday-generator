openapi: 3.0.0
info:
  title: Test API
  description: Test API
  version: 1.0.0

components:
  schemas:

    Test:
      type: object
      properties:
        value:
          type: string
      required: [value]

paths: {}
