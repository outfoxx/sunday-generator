openapi: 3.0.0
info:
  title: Test API
  description: Test API
  version: 1.0.0
servers:
- url: 'https://example.com/api/'


components:
  schemas:

    Test2:
      allOf:
      - $ref: './lib-obj-inherits.yaml#/components/schemas/Test'
      - properties:
          value2:
            type: string
        required: [value2]

    Test3:
      x-sunday-super: { $ref: '#/components/schemas/Test2' }
      allOf:
      - $ref: '#/components/schemas/Test2'
      - properties:
          value3:
            type: string
        required: [value3]


paths:
  /test:
    get:
      summary: Get Test Object
      operationId: getTest
      responses:
        "200":
          description: Current test object
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Test3' }
