openapi: 3.0.0
info:
  title: Test API
  description: Test API
  version: 1.0.0
servers:
- url: 'https://example.com/api/'

x-sunday-javaModelPackage: io.explicit.test

components:
  schemas:

    Value:
      type: string
      x-sunday-kotlinType: java.time.LocalDateTime
      x-sunday-kotlinType-server: java.time.Instant

    Test:
      type: object
      properties:
        value: { $ref: '#/components/schemas/Value' }
      required: [value]


paths:
  /test:
    get:
      summary: Get Test Object
      operationId: getTest
      responses:
        "200":
          description: Current test object
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Test' }
