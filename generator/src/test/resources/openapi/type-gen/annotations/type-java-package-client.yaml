openapi: 3.0.0
info:
  title: Test API
  description: Test API
  version: 1.0.0
servers:
- url: 'https://example.com/api/'

x-sunday-javaPackage: io.explicit.test
x-sunday-javaPackage-client: io.explicit.test.client

paths:
  /test:
    get:
      summary: Get Test Object
      operationId: getTest
      responses:
        "200":
          description: Current test object
          content:
            application/json:
              schema:
                type: object
                properties:
                  value:
                    type: string
                required: [value]
