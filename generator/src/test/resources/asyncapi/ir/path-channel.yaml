asyncapi: "2.6.0"
info:
  title: Event API
  version: "1.0.0"
channels:
  /events:
    subscribe:
      operationId: streamEvents
      message:
        payload:
          type: object
          required: [id]
          properties:
            id:
              type: string
